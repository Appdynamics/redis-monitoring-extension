# List of Redis Servers
servers:
  - host: "localhost"
    port: 6379
    password: ""
    displayName: "localhost"

    includePatterns: [.*Stats|.*
               ]
    excludePatterns: [
           ]

#prefix used to show up metrics in AppDynamics
metricPrefix:  "Custom Metrics|Redis|"
